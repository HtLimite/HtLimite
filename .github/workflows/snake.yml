name: Contribution Snake Disabled

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Show current snake source
        run: echo "Snake image is now served from assets/github-contribution-grid-snake.svg in the main branch."
